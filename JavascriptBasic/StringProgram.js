// Program to reverse the all words in  a given string
let str="welcome to tcs";
let emptyStr="";
for(let x of str){
    // emptyStr= x + emptyStr.trim();// for remove the space 
    emptyStr= x + emptyStr;
    //console.log(emptyStr);

}
console.log("Final Output is :- ",emptyStr);
